import type { ReactNode } from 'react';
export default function Magnet({children}:{children:ReactNode;padding?:number;magnetStrength?:number}){return <div style={{display:'inline-flex'}}>{children}</div>}
