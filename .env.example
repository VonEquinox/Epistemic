# Database
DATABASE_URL=postgres://epistemic:epistemic@localhost:5432/epistemic

# API
API_HOST=0.0.0.0
API_PORT=8080
SESSION_SECRET=change-me-to-a-long-random-string
SESSION_SECURE=false
RUST_LOG=info,epistemic=debug

# Bootstrap first admin (only when users table is empty)
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_NAME=Admin
BOOTSTRAP_ADMIN_PASSWORD=changeme123

# Paths
PDF_DIR=./data/pdfs
# Optional legacy TEI dir (read-only if old parses exist). GROBID removed.
TEI_DIR=./data/tei

# LLM — OpenAI Chat Completions compatible (OpenAI / gateway / vLLM / OpenRouter)
# Base URL should be the API root; client appends /v1 if missing, then /chat/completions
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o
# Aliases also accepted: LLM_API_KEY, LLM_BASE_URL, LLM_MODEL

# Embedding provider (undecided: voyage | local | openai)
EMBEDDING_PROVIDER=voyage
VOYAGE_API_KEY=
EMBEDDING_DIM=1024
